/// <reference types="@lynx-js/rspeedy/client" />

// `<x-lynx-fast-image>` is typed by `lynx-fast-image` itself (src/jsx.d.ts,
// pulled in transitively). Prefer the `<FastImage>` wrapper over the raw tag.
